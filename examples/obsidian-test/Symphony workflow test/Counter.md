# Counter

Show a prominent count starting at zero and an Add button that increments it
by one. The button must work with keyboard and mouse. Refreshing the page may
reset the value to zero. Include tests for the initial value and increment.

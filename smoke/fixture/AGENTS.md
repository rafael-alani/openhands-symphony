# Smoke repository instructions

Keep `calculator.py` dependency-free. Add or update `unittest` coverage for every behavior change. Run `python3 -m unittest discover -s tests -v`. Do not use GitHub commands from an agent workspace.
